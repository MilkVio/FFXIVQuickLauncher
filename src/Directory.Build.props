<!-- Contains required properties for XIVLauncher projects. -->
<Project>
    <PropertyGroup Label="C#">
        <LangVersion>latest</LangVersion>
        <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
    </PropertyGroup>
    <PropertyGroup Label="Native Shims">
        <!-- Keep the normal local C# debug loop usable without a Rust toolchain; CI/release still builds native shims. -->
        <SkipNativeShims Condition="'$(SkipNativeShims)' == '' and '$(Configuration)' == 'Debug' and '$(CI)' != 'true'">true</SkipNativeShims>
        <SkipNativeShims Condition="'$(SkipNativeShims)' == ''">false</SkipNativeShims>
    </PropertyGroup>
    <PropertyGroup Label="Documentation">
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
        <NoWarn>$(NoWarn);CS1591</NoWarn>
    </PropertyGroup>
    <PropertyGroup Label="Project">
        <!--
        NU1701:
          DeepEqual is not netstandard-compatible. This is fine since we run tests with .NET Framework anyway.
          This is required due to https://github.com/NuGet/Home/issues/5740
        -->
        <NoWarn>$(NoWarn);NU1701</NoWarn>
    </PropertyGroup>
    <PropertyGroup Label="Nuget">
        <IsPackable>false</IsPackable>
        <Authors>XIVLauncher Contributors</Authors>
        <PackageLicenseExpression>GPLv3</PackageLicenseExpression>
        <PackageProjectUrl>https://github.com/AtmoOmen/FFXIVQuickLauncher</PackageProjectUrl>
        <RepositoryUrl>https://github.com/AtmoOmen/FFXIVQuickLauncher</RepositoryUrl>
        <PackageReleaseNotes>Automated release</PackageReleaseNotes>
        <Company>goatsoft</Company>
        <Copyright>Copyright (c) goatsoft</Copyright>
        <PackageTags>mmo</PackageTags>
    </PropertyGroup>
</Project>
