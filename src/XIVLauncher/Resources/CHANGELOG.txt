* 调整为 Violet 版维护构建，并更新 README、关于页面与相关品牌标识防止误识别
* 启动器自动更新源改为 MilkVio/FFXIVQuickLauncher 的 GitHub Release
* 新增 扫码登陆时可以选择是否启用独立机器码
* 新增“所有账号不启用轮换机器码”全局开关，可在设置中统一跳过账号机器码轮换
* 修正 Violet 发布版本规则，后续发布使用三段版本号以兼容 Velopack 更新机制
* 更新 Release 构建流程，发布时自动生成 GitHub Release、Velopack 更新清单与便携版压缩包

> 当前分支仅支持 `Windows 11` 的 `x64` 环境，不提供 `Linux`、`macOS` 与 `SteamDeck` 等环境的相关支持
