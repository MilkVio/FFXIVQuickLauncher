<UserControl
    x:Class="XIVLauncher.Xaml.Components.FolderEntry"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Foreground="{DynamicResource MaterialDesign.Brush.Foreground}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBox
            MinHeight="48"
            Padding="12,8"
            Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
            VerticalContentAlignment="Center"
            materialDesign:HintAssist.Hint="{Binding Description, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
            materialDesign:HintAssist.IsFloating="True"
            Style="{StaticResource MaterialDesignFloatingHintTextBox}"
            Text="{Binding Text, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, UpdateSourceTrigger=PropertyChanged}"
            TextChanged="TextBoxBase_OnTextChanged" />

        <Button
            Grid.Column="1"
            Width="48"
            Height="48"
            Margin="8,0,0,0"
            Padding="0"
            Click="BrowseFolder"
            Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
            Style="{StaticResource MaterialDesignOutlinedButton}"
            ToolTip="浏览文件夹"
            materialDesign:ButtonAssist.CornerRadius="6">
            <materialDesign:PackIcon Width="20" Height="20" Kind="FolderOpenOutline" />
        </Button>
    </Grid>
</UserControl>
