<UserControl x:Class="XIVLauncher.Windows.Main.Slides.ScanQRSlide"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="240">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Windows/Main/LoginCardSlideResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="24,12,24,8" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="扫码登录"
                   HorizontalAlignment="Center"
                   FontSize="16"
                   Margin="0,0,0,8" />
        <TextBlock Grid.Row="1"
                   Text="请打开手机叨鱼应用程序扫码登录"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   FontSize="12"
                   Opacity="0.7"
                   Margin="0,0,0,12" />

        <Border Grid.Row="2"
                Background="White"
                CornerRadius="8"
                ClipToBounds="True"
                HorizontalAlignment="Center"
                Margin="0,0,0,16">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"
                                  RenderingBias="Performance" />
            </Border.Effect>
            <Grid Width="136" Height="136">
                <Image x:Name="QrImage"
                       Margin="8"
                       Stretch="Uniform"
                       Source="{Binding LoginPage.QRCodeBitmapImage}" />
                <Grid
                    Visibility="{Binding LoginPage.IsQrCodeExpired, Converter={StaticResource BoolToVis}}"
                    Background="Transparent"
                    Cursor="Hand">
                    <Grid.InputBindings>
                        <MouseBinding MouseAction="LeftClick"
                                      Command="{Binding LoginPage.RefreshQrCodeCommand}" />
                    </Grid.InputBindings>
                    <Border x:Name="OverlayBorder" CornerRadius="8">
                        <Border.Background>
                            <SolidColorBrush Color="#99000000" />
                        </Border.Background>
                    </Border>
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <materialDesign:PackIcon x:Name="RefreshIcon"
                                                 Kind="Refresh"
                                                 Foreground="White"
                                                 HorizontalAlignment="Center"
                                                 Width="32" Height="32" Opacity="0.9"
                                                 RenderTransformOrigin="0.5,0.5">
                            <materialDesign:PackIcon.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform ScaleX="0.85" ScaleY="0.85" />
                                    <RotateTransform Angle="0" />
                                </TransformGroup>
                            </materialDesign:PackIcon.RenderTransform>
                        </materialDesign:PackIcon>
                        <TextBlock x:Name="RefreshText"
                                   Text="点击刷新"
                                   Foreground="White"
                                   FontSize="12"
                                   Margin="0,4,0,0"
                                   Opacity="0.9"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                    <Grid.Triggers>
                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation
                                        Storyboard.TargetName="OverlayBorder"
                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                        To="#B3000000" Duration="0:0:0.15" />
                                    <DoubleAnimation
                                        Storyboard.TargetName="RefreshIcon"
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                        To="1" Duration="0:0:0.15" />
                                    <DoubleAnimation
                                        Storyboard.TargetName="RefreshIcon"
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                        To="1" Duration="0:0:0.15" />
                                    <DoubleAnimation
                                        Storyboard.TargetName="RefreshIcon"
                                        Storyboard.TargetProperty="Opacity"
                                        To="1" Duration="0:0:0.15" />
                                    <DoubleAnimation
                                        Storyboard.TargetName="RefreshText"
                                        Storyboard.TargetProperty="Opacity"
                                        To="1" Duration="0:0:0.15" />
                                    <DoubleAnimation
                                        Storyboard.TargetName="RefreshIcon"
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(RotateTransform.Angle)"
                                        By="90" Duration="0:0:0.15" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation
                                        Storyboard.TargetName="OverlayBorder"
                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                        To="#99000000" Duration="0:0:0.15" />
                                    <DoubleAnimation
                                        Storyboard.TargetName="RefreshIcon"
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                        To="0.85" Duration="0:0:0.15" />
                                    <DoubleAnimation
                                        Storyboard.TargetName="RefreshIcon"
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                        To="0.85" Duration="0:0:0.15" />
                                    <DoubleAnimation
                                        Storyboard.TargetName="RefreshIcon"
                                        Storyboard.TargetProperty="Opacity"
                                        To="0.9" Duration="0:0:0.15" />
                                    <DoubleAnimation
                                        Storyboard.TargetName="RefreshText"
                                        Storyboard.TargetProperty="Opacity"
                                        To="0.9" Duration="0:0:0.15" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Grid.Triggers>
                </Grid>
            </Grid>
        </Border>

        <Button x:Name="BackToMain"
                Grid.Row="3"
                Style="{StaticResource MaterialDesignOutlinedButton}"
                materialDesign:ButtonAssist.CornerRadius="16"
                HorizontalAlignment="Center"
                Width="130"
                Height="32"
                ToolTip="返回主页面"
                Command="{Binding LoginPage.BackToMainPageCommand}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="ArrowLeft" Margin="0,0,8,0"
                                         VerticalAlignment="Center" />
                <TextBlock Text="返回" VerticalAlignment="Center" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
